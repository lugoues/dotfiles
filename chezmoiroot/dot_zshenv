# Managed by chezmoi. Renders to ~/.zshenv.
export ZDOTDIR="$HOME/.local/etc/zsh"

# Guarded: on a partial or interrupted apply this file can land before the one
# it points at, and an unreadable path here would break every shell on the box.
[[ -r "$ZDOTDIR/.zshenv" ]] && . "$ZDOTDIR/.zshenv"